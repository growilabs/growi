// Transpiled CJS (`exports.default`); only executed through bundler interop
// (client-only renderer plugin). Redeclared as `export =` of the function.
import Orig from 'csv-to-markdown-table/lib/CsvToMarkdown.js';
declare const csvToMarkdown: typeof Orig;
export = csvToMarkdown;
