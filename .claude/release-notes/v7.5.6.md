### 💎 Features

* feat: Contribution Graph on the user's profile (#10509) @arvid-e
  - Adds a heatmap contribution graph to user profiles, tracking page and comment activity in real time via a dedicated collection with O(1) read performance and a one-time migration for historical data.
  - ユーザープロフィールに貢献グラフ（ヒートマップ）を追加。ページ・コメントのアクティビティを専用コレクションでリアルタイム記録し、過去のデータも一括移行されます。
* feat(comment): Comment mention and add In-App-notification (#11058) @Ryosei-Fukushima
  - Adds @username mention support in page comments: mentioned users receive in-app notifications, mentions are highlighted in blue in both the editor and rendered view, and an autocomplete list appears while typing.
  - コメントで `@username` によるメンションをサポート。メンションされたユーザーへインアプリ通知が届き、エディター・表示の両方で青色ハイライト表示されます。入力中はオートコンプリート候補も表示されます。
* feat: Changes Index and Revision Diff APIs (#11353) @yuki-takei
  - Adds `GET /api/v3/revisions/changes` returning a user's edit history as consecutive-edit "runs" with cursor pagination, and `POST /api/v3/revisions/diff` for batch unified diff of up to 20 revision pairs with per-pair access control.
  - ユーザーの連続編集履歴をカーソルページネーションで取得する `GET /api/v3/revisions/changes` と、最大 20 ペアのリビジョン差分を一括取得する `POST /api/v3/revisions/diff` を追加。各ペアのアクセス制御も個別に適用されます。
* feat: Add button to make all notification as read (#11352) @ryotaro-nagahara
  - Adds a "Mark all as read" button to the in-app notification panel; optimistic SWR updates make read/unread status reflect instantly in the UI including sidebar badges.
  - インアプリ通知パネルに「すべて既読にする」ボタンを追加。SWR 楽観更新により、既読状態がサイドバーバッジを含む UI にリアルタイムで反映されます。

### 🚀 Improvement

* imprv: Expose news body (#11317) @ryotaro-nagahara
  - Adds a dedicated `/_news` page listing all GROWI news with full article bodies and external links; clicking a news notification now navigates directly to the relevant item on this page.
  - ニュース全文を表示する `/_news` ページを新設。インアプリ通知のニュースアイテムをクリックすると該当記事へ直接ジャンプし、外部リンクアイコンも表示されます。
* imprv: Include page revert action in supported contribution actions (#11321) @arvid-e
  - Page revert (restoring a previous revision) is now counted as a contribution action and reflected in the user contribution graph.
  - ページのリバート操作（過去リビジョンへの復元）が貢献アクションとして集計され、コントリビューショングラフに反映されます。
* imprv: Page create button interaction (#11313) @hikaru-n-cpu
  - Improves the responsiveness and intuitiveness of the page creation button interaction.
  - 新規ページ作成ボタンのインタラクションを改善し、より直感的な操作感を実現。

### 🐛 Bug Fixes

* fix: Suppress modifier shortcuts in editable elements (#11340) @yuki-takei
  - 編集可能な要素内で修飾キーショートカットが誤って発火する問題を修正。
* fix: escape \</style> breakout in renderCustomCss (#11298) @yuki-takei
  - カスタム CSS レンダリングで `</style>` によるタグ逃げ出しを防ぐよう修正。
* fix: Use req.user.username in LDAP Execute Sync instead of req.user.name (#11356) @yuki-takei
  - LDAP 同期実行時に `req.user.name` の代わりに `req.user.username` を使用するよう修正。
* feat(page-bulk-export): Rework bulk PDF/HTML export to render Markdown like the web (tables, math, emoji, directives) (#11288) @yuki-takei
  - バルク PDF/HTML エクスポートで、テーブル・数式・絵文字・ディレクティブを Web 表示と同様にレンダリングするよう改修。

### 🧰 Maintenance

* ci(deps): bump js-yaml from 4.1.1 to 4.2.0 (#11311) @[dependabot[bot]](https://github.com/apps/dependabot)
* chore(deps): dedupe pnpm-lock.yaml to CI-gate release dependency churn (#11343) @yuki-takei
  - Runs `pnpm dedupe` on a dedicated branch so CI validates lock-file dependency upgrades before they reach production, preventing untested dependency churn in release PRs.
  - リリース PR に含まれる依存関係の更新が CI で検証されないまま master に入る問題を解消するため、`pnpm dedupe` を通常ブランチ上で実行。
* ci(deps-dev): bump vitest from 3.2.4 to 3.2.6 (#11279) @[dependabot[bot]](https://github.com/apps/dependabot)
* ci(deps): bump form-data from 4.0.5 to 4.0.6 (#11335) @[dependabot[bot]](https://github.com/apps/dependabot)

<!-- pv-jp-en-translated -->
