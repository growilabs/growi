# Search Filter Implementation Rules
- All new source code must be created in `apps/app/src/features/search`.
- Use functional React components.
- Ensure all filters are compatible with the existing `SearchService`.
- Follow the 'Plugin' pattern defined in `.kiro/specs/search-filters/framework`.