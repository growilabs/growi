{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["dist/**"],
      "outputLogs": "new-only"
    },
    "dev": {
      "outputs": ["dist/**"],
      "outputLogs": "new-only"
    }
  }
}
