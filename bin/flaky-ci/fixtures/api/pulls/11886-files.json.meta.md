# Source

- **Real.** `gh api -X GET repos/growilabs/growi/pulls/11886/files --paginate`.
- PR [#11886](https://github.com/growilabs/growi/pull/11886) — "ci(deps):
  bump multer from 1.4.4 to 2.3.0" (a dependabot PR, unmerged/open). Its
  `pnpm-lock.yaml` patch is one of the real PRs issue #11849's
  `### 原因確定` root-cause comment names as having regenerated the lockfile
  in a way that resolved `@codemirror/state` to two versions (6.7.1 direct
  vs. 6.7.4 via `@codemirror/view`) — see
  `bin/flaky-ci/fixtures/README.md` → "The `@codemirror/state` lockfile
  PR" for why this specific PR (of the four named: #11846, #11886, #11887,
  #11888) was picked.
- Contains 2 files: `apps/app/package.json` and `pnpm-lock.yaml` (the one
  used by `lockfile-overlap`).
- **Captured**: 2026-09-16.
