# Source

- **Real.** `gh api -X GET repos/growilabs/growi/pulls/11919/files --paginate`.
- PR [#11919](https://github.com/growilabs/growi/pull/11919) — "feat: Sync
  backlinks on page delete" (open at capture time). 5 files, including
  `apps/app/src/features/backlinks/server/services/page-link-sync.ts`, used
  as the real "spec path matches (by suffix)" case for `pr-owns-failure.ts`
  with `--spec-path src/features/backlinks/server/services/page-link-sync.ts`.
  The same fixture, with an unrelated `--spec-path`, is the real
  "spec path doesn't match" case.
- **Captured**: 2026-09-16.
