# `11720-body.md`

- **Source**: real. Issue **#11720** (`flaky-ci-routine: dashboard`), the body
  the routine itself wrote on its 2026-09-16T00:12:47Z run, fetched verbatim
  with `gh api repos/growilabs/growi/issues/11720 -q '.body'`.
- **Captured**: 2026-09-16.
- **What it pins**: the "before" output of task 3.10 — what Step 5's prose
  procedure actually produced. `lib/dashboard.spec.ts` renders
  `render-dashboard-input.json` and compares the result against this file
  character for character (Requirement 1.3), so a change in the row order, a
  column, a cell rule or one of the three fixed lines fails the test.
- `gh api -q` appends a newline that GitHub does not store; the test strips
  one trailing newline before comparing.
