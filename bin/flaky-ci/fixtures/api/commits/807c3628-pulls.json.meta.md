# Source

- **Real.** `gh api -X GET repos/growilabs/growi/commits/807c3628fc85bbf29335840d004660ce8c195f64/pulls`.
- Full raw response (1 PR: #11919, base `feat/185872-backlinks`, state
  `open`). Used together with `../compare/master-diverged-807c3628.json`
  and `../pulls/11919-files.json` as the "ancestor doesn't exist, PR exists"
  test rows (both the matching- and non-matching-spec-path cases reuse this
  same PR's file list with different `--spec-path` values).
- **Captured**: 2026-09-16.
