# Source

- **Constructed** (see `constructed-empty-pulls.json.meta.md`). Covers the
  procedure's "Mergify can batch several PRs into one queue commit" note —
  two `Merge of #{N}` lines in one commit message, both of which
  `pr-owns-failure.ts` must extract.
- **Captured**: 2026-09-16 (constructed, not fetched).
