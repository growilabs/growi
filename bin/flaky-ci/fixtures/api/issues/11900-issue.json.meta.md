# Source

- **Real.** `gh api -X GET repos/growilabs/growi/issues/11900`.
- Issue [#11900](https://github.com/growilabs/growi/issues/11900) — body has
  a `### First observation` section with a `Date:` line, and (verified via
  `11900-comments.json`) **no** `### Additional observation` /
  `### Backfilled observation` comments — the "body-only observation" case
  for `newest-observation`.
- **Captured**: 2026-09-16.
